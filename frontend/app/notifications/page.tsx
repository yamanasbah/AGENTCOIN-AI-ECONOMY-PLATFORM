'use client';import { useNotifications,useReadNotification } from '@/hooks/use-platform';
export default function NotificationsPage(){const {data,refetch}=useNotifications();const{mutateAsync}=useReadNotification();return <div className='space-y-4'><h2 className='text-2xl font-semibold'>Notifications</h2>{(data||[]).map((item)=><div key={item.id} className='rounded border border-zinc-800 p-4'><div className='flex items-center justify-between'><h3 className='font-semibold'>{item.title}</h3>{!item.read&&<button className='rounded bg-zinc-800 px-2 py-1 text-xs' onClick={async()=>{await mutateAsync(item.id);await refetch();}}>Mark read</button>}</div><p className='text-zinc-300'>{item.message}</p></div>)}</div>;}
